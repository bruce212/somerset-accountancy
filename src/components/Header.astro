---
const links = [
  { href: "/", label: "Home" },
  { href: "/services", label: "Services" },
  { href: "/about", label: "About" },
  { href: "/contact", label: "Contact" },
];
const path = Astro.url.pathname;
---

<header class="site-header">
  <div class="container header-inner">
    <a class="brand" href="/">
      <span class="brand-mark">SA</span>
      <span class="brand-name">Somerset Accountancy</span>
    </a>
    <nav aria-label="Primary">
      <ul>
        {
          links.map((link) => (
            <li>
              <a
                href={link.href}
                class:list={[
                  "nav-link",
                  { active: path === link.href || (link.href !== "/" && path.startsWith(link.href)) },
                ]}
              >
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
    <a class="button button-primary header-cta" href="/contact">Book a consultation</a>
  </div>
</header>

<style>
  .site-header {
    background: var(--colour-surface);
    border-bottom: 1px solid var(--colour-border);
    position: sticky;
    top: 0;
    z-index: 50;
  }

  .header-inner {
    display: flex;
    align-items: center;
    gap: 2rem;
    padding: 1rem 1.25rem;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: var(--colour-ink);
    font-weight: 600;
  }

  .brand:hover {
    color: var(--colour-ink);
  }

  .brand-mark {
    font-family: var(--font-serif);
    background: var(--colour-ink);
    color: var(--colour-bg);
    padding: 0.4rem 0.6rem;
    border-radius: var(--radius);
    font-size: 0.95rem;
    letter-spacing: 0.02em;
  }

  .brand-name {
    font-family: var(--font-serif);
    font-size: 1.1rem;
  }

  nav {
    margin-left: auto;
  }

  nav ul {
    display: flex;
    gap: 1.75rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-link {
    color: var(--colour-ink-soft);
    font-size: 0.95rem;
    font-weight: 500;
    padding: 0.25rem 0;
    border-bottom: 2px solid transparent;
  }

  .nav-link:hover,
  .nav-link.active {
    color: var(--colour-ink);
    border-bottom-color: var(--colour-accent);
  }

  .header-cta {
    padding: 0.55rem 1.1rem;
    font-size: 0.9rem;
  }

  @media (max-width: 820px) {
    .brand-name,
    .header-cta {
      display: none;
    }
    nav ul {
      gap: 1.25rem;
    }
  }
</style>
